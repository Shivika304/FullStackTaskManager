## 🧠 Project Overview
This project is a full-stack Task Manager application that enables users to manage daily tasks through a web interface. It supports basic CRUD operations and uses RESTful APIs for communication between frontend and backend.

The backend is built using Spring Boot, while the frontend is developed using HTML, CSS, and JavaScript. Task data is stored in a MySQL database.
